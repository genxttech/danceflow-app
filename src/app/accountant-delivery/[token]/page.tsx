import { notFound } from "next/navigation";
import { createAdminClient } from "@/lib/supabase/admin";
import { hashAccountantDeliveryToken } from "@/lib/accountant-deliveries/tokens";
const labels: Record<string,string>={profit_loss:"Profit & loss",accounting_ledger:"Accounting ledger",payments_refunds:"Payments and refunds",expenses:"Expenses",event_profitability:"Event profitability"};
export default async function Page({params}:{params:Promise<{token:string}>}){const {token}=await params;const supabase=createAdminClient();const {data}=await supabase.from("studio_accountant_deliveries").select("id,recipient_email,report_types,report_range,status,expires_at,studios(name)").eq("token_hash",hashAccountantDeliveryToken(token)).maybeSingle();if(!data||data.status==="cancelled"||new Date(data.expires_at).getTime()<Date.now()) notFound();const studio=Array.isArray(data.studios)?data.studios[0]:data.studios;return <main className="mx-auto max-w-3xl p-6"><div className="rounded-2xl border bg-white p-6 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wider text-violet-700">Secure accounting package</p><h1 className="mt-2 text-3xl font-semibold">{studio?.name??"DanceFlow studio"} reports</h1><p className="mt-2 text-sm text-slate-600">Prepared for {data.recipient_email}. This private link expires {new Date(data.expires_at).toLocaleString()}.</p><div className="mt-6 space-y-3">{(data.report_types??[]).map((type:string)=><a key={type} href={`/accountant-delivery/${token}/download/${type}`} className="block rounded-xl border px-4 py-3 font-semibold text-violet-700 hover:bg-violet-50">Download {labels[type]??type}</a>)}</div><p className="mt-6 text-xs text-slate-500">Do not forward this link. Contact the studio if access should be revoked or extended.</p></div></main>}
